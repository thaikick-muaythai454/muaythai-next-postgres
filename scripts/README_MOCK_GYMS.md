# 📋 คู่มือเพิ่มค่ายมวยจำลอง

## วิธีเพิ่มค่ายมวยจำลองเข้าไปใน Supabase

### วิธีที่ 1: ใช้ Supabase SQL Editor (แนะนำ)

1. เปิด Supabase Dashboard
2. ไปที่ **SQL Editor**
3. คัดลอกเนื้อหาจากไฟล์ `scripts/add-mock-gyms.sql`
4. Paste ลงใน SQL Editor
5. กด **Run** หรือ **Execute**

### วิธีที่ 2: ใช้ Supabase CLI

```bash
# รัน SQL script
supabase db execute -f scripts/add-mock-gyms.sql
```

### วิธีที่ 3: ใช้ pgAdmin หรือ Database Client

1. เชื่อมต่อกับ Supabase database
2. เปิดไฟล์ `scripts/add-mock-gyms.sql`
3. Execute SQL script

---

## 📦 ข้อมูลที่เพิ่มเข้าไป

สคริปต์จะเพิ่ม **5 ค่ายมวยจำลอง** พร้อมแพ็คเกจ:

### 1. ค่ายมวยราชดำเนิน (Bangkok)
- **Location**: กรุงเทพฯ
- **Type**: Traditional
- **Packages**: Day Pass, 1 Month

### 2. ค่ายมวยดอยสุเทพ (Chiang Mai)
- **Location**: เชียงใหม่
- **Type**: Traditional
- **Packages**: 1 Week (with accommodation), 1 Month

### 3. ค่ายมวยพัทยา (Pattaya)
- **Location**: พัทยา
- **Type**: Modern
- **Packages**: Day Pass, 1 Week, 1 Month

### 4. ค่ายมวยภูเก็ต บีช (Phuket)
- **Location**: ภูเก็ต
- **Type**: Modern
- **Packages**: 1 Week (with accommodation), 1 Month

### 5. ค่ายมวยอีสาน ฟิตเนส (Khon Kaen)
- **Location**: ขอนแก่น
- **Type**: Modern
- **Packages**: Day Pass, 1 Month, 3 Months

---

## 🔍 ตรวจสอบผลลัพธ์

หลังจากรันสคริปต์แล้ว สามารถตรวจสอบได้:

### ใน Supabase Dashboard

```sql
-- ดูค่ายมวยทั้งหมด
SELECT 
  gym_name,
  gym_name_english,
  location,
  status,
  slug
FROM gyms
WHERE status = 'approved'
ORDER BY created_at DESC;
```

### ในแอปพลิเคชัน

1. เปิด `/gyms` page
2. ควรเห็นค่ายมวยจำลองทั้งหมดที่เพิ่มเข้าไป

---

## ⚙️ การกำหนดค่า

สคริปต์จะ:
- หา system user หรือ admin user อัตโนมัติ
- ถ้าไม่มี user จะสร้าง system user ใหม่
- เพิ่มค่ายมวยพร้อมแพ็คเกจ
- ตั้ง status เป็น `approved` ทันที (แสดงผลใน /gyms)

---

## 🛠️ แก้ไขข้อมูล

ถ้าต้องการแก้ไขข้อมูลค่ายมวย:

```sql
-- แก้ไขชื่อค่ายมวย
UPDATE gyms
SET gym_name = 'ชื่อใหม่'
WHERE slug = 'rajadamnern-muay-thai-gym';

-- แก้ไขราคาแพ็คเกจ
UPDATE gym_packages
SET price = 5000.00
WHERE gym_id = (SELECT id FROM gyms WHERE slug = 'rajadamnern-muay-thai-gym')
  AND name = 'แพ็คเกจ 1 เดือน';
```

---

## ⚠️ หมายเหตุ

- สคริปต์ใช้ `ON CONFLICT DO NOTHING` เพื่อป้องกันการเพิ่มซ้ำ
- ถ้า slug ซ้ำจะไม่เพิ่มข้อมูล
- สามารถรันสคริปต์หลายครั้งได้โดยไม่เกิด error

---

## 📝 เพิ่มค่ายมวยใหม่

ถ้าต้องการเพิ่มค่ายมวยใหม่ สามารถ copy format จากสคริปต์:

```sql
INSERT INTO gyms (
  user_id,
  gym_name,
  gym_name_english,
  contact_name,
  phone,
  email,
  location,
  address,
  gym_details,
  latitude,
  longitude,
  map_url,
  socials,
  gym_type,
  slug,
  status,
  services,
  created_at,
  updated_at
) VALUES (
  v_system_user_id,  -- ใช้ user_id จากตัวแปร
  'ชื่อค่ายมวย',
  'English Name',
  'ชื่อผู้ติดต่อ',
  '+66 XX XXX XXXX',
  'email@example.com',
  'Location',
  'ที่อยู่',
  'รายละเอียด',
  13.7563,  -- latitude
  100.5018, -- longitude
  'https://maps.google.com/?q=13.7563,100.5018',
  'facebook.com/page',
  'Traditional', -- หรือ 'Modern'
  'gym-slug',    -- ต้อง unique
  'approved',
  ARRAY['บริการ1', 'บริการ2'],
  NOW(),
  NOW()
) ON CONFLICT (slug) DO NOTHING;
```

---

**Last Updated**: 2025-01-30

