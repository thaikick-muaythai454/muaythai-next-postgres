# 🚀 คู่มือการติดตั้งและใช้งาน Muaythai Next.js + Supabase

## เริ่มต้นอย่างรวดเร็ว (Quick Start)

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. ตั้งค่า Supabase

#### สร้าง Supabase Project (ฟรี)

1. ไปที่ [https://app.supabase.com](https://app.supabase.com)
2. คลิก **"New Project"**
3. กรอกข้อมูล:
   ```
   Organization: เลือกหรือสร้างใหม่
   Name: muaythai-project (หรือชื่ออื่นที่ต้องการ)
   Database Password: สร้างรหัสผ่านที่แข็งแรง (จดเก็บไว้ด้วย)
   Region: Southeast Asia (Singapore) - แนะนำสำหรับประเทศไทย
   Pricing Plan: Free ($0 per month)
   ```
4. คลิก **"Create new project"**
5. รอประมาณ 1-2 นาทีให้ project พร้อมใช้งาน

#### คัดลอก API Keys

1. ในหน้า Project Dashboard
2. คลิกที่ **Settings** (⚙️ ด้านล่างซ้าย)
3. เลือก **API** จากเมนู
4. คัดลอกค่าเหล่านี้:
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public key**: `eyJhbGci...` (คีย์ยาวๆ)

### 3. ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local`:

```bash
cp .env.example .env.local
```

แก้ไขไฟล์ `.env.local` ใส่ค่าที่คัดลอกมา:

```env
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGci...
```

### 4. รัน Development Server

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่ [http://localhost:3000](http://localhost:3000)

---

## 📚 ตัวอย่างการใช้งาน

### Todo App - CRUD Operations

#### 1. สร้างตาราง `todos` ใน Supabase

1. ไปที่ Supabase Dashboard
2. คลิก **SQL Editor** ในเมนูซ้าย
3. คลิก **"+ New query"**
4. วางโค้ด SQL นี้:

```sql
-- สร้างตาราง todos
CREATE TABLE todos (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  task TEXT NOT NULL,
  is_complete BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

-- เปิดใช้งาน Row Level Security
ALTER TABLE todos ENABLE ROW LEVEL SECURITY;

-- สร้าง Policies
CREATE POLICY "Anyone can read todos" ON todos FOR SELECT TO public USING (true);
CREATE POLICY "Anyone can insert todos" ON todos FOR INSERT TO public WITH CHECK (true);
CREATE POLICY "Anyone can update todos" ON todos FOR UPDATE TO public USING (true);
CREATE POLICY "Anyone can delete todos" ON todos FOR DELETE TO public USING (true);
```

5. คลิก **"Run"** (หรือกด `Cmd/Ctrl + Enter`)
6. ดู message "Success. No rows returned"
7. ไปที่ [http://localhost:3000/examples/todos](http://localhost:3000/examples/todos) เพื่อทดสอบ

#### 2. ทดสอบ Todo App

- เพิ่มงานใหม่
- คลิก checkbox เพื่อทำเครื่องหมายว่าเสร็จแล้ว
- ลบงาน
- Refresh หน้า - ข้อมูลจะยังคงอยู่ใน database!

---

### Authentication - ระบบ Login/Register

#### 1. ตั้งค่า Authentication (Optional สำหรับ Development)

ในโหมด development Supabase จะส่งอีเมลยืนยัน ถ้าต้องการทดสอบโดยไม่ต้องยืนยันอีเมล:

1. ไปที่ Supabase Dashboard
2. คลิก **Authentication** ในเมนูซ้าย
3. เลือก **Settings**
4. หาส่วน **"Email Auth"**
5. **ปิด** "Enable email confirmations"
6. คลิก **Save**

#### 2. ทดสอบ Authentication

1. ไปที่ [http://localhost:3000/examples/auth](http://localhost:3000/examples/auth)
2. คลิกแท็บ **"ลงทะเบียน"**
3. กรอก:
   - อีเมล: `test@example.com`
   - รหัสผ่าน: `password123` (ต้องมีอย่างน้อย 6 ตัว)
4. คลิก **"ลงทะเบียน"**
5. ถ้าปิด email confirmation แล้ว จะเข้าสู่ระบบทันที
6. ลอง **ออกจากระบบ** และ **เข้าสู่ระบบ** ใหม่

---

## 🗃️ ตัวอย่าง SQL เพิ่มเติม

### Profiles Table - ข้อมูลผู้ใช้เพิ่มเติม

```sql
CREATE TABLE profiles (
  id UUID REFERENCES auth.users ON DELETE CASCADE PRIMARY KEY,
  username TEXT UNIQUE,
  full_name TEXT,
  avatar_url TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Public profiles are viewable" ON profiles FOR SELECT TO public USING (true);
CREATE POLICY "Users can update own profile" ON profiles FOR UPDATE TO authenticated USING (auth.uid() = id);
```

### Posts Table - ตัวอย่าง Blog/Social

```sql
CREATE TABLE posts (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  user_id UUID REFERENCES auth.users ON DELETE CASCADE NOT NULL,
  title TEXT NOT NULL,
  content TEXT,
  published BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL
);

ALTER TABLE posts ENABLE ROW LEVEL SECURITY;

-- เฉพาะโพสต์ที่ publish แล้วจึงจะเห็นได้
CREATE POLICY "Public posts are viewable" ON posts FOR SELECT TO public USING (published = true);

-- เจ้าของเห็นโพสต์ของตัวเองทั้งหมด
CREATE POLICY "Users can view own posts" ON posts FOR SELECT TO authenticated USING (auth.uid() = user_id);

-- เจ้าของสร้างและแก้ไขโพสต์ของตัวเองได้
CREATE POLICY "Users can create posts" ON posts FOR INSERT TO authenticated WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update own posts" ON posts FOR UPDATE TO authenticated USING (auth.uid() = user_id);
CREATE POLICY "Users can delete own posts" ON posts FOR DELETE TO authenticated USING (auth.uid() = user_id);
```

---

## 🔧 คำสั่งที่สำคัญ

```bash
# Development
npm run dev          # รัน dev server (http://localhost:3000)

# Production
npm run build        # Build production
npm start            # รัน production server

# Code Quality
npm run lint         # ตรวจสอบ code ด้วย ESLint
npm run type-check   # ตรวจสอบ TypeScript types (ถ้ามี)
```

---

## 🐛 แก้ปัญหาที่พบบ่อย (Troubleshooting)

### ❌ Error: "relation does not exist"

**สาเหตุ**: ยังไม่ได้สร้างตารางในฐานข้อมูล

**แก้ไข**: รัน SQL สำหรับสร้างตาราง (ดูด้านบน)

---

### ❌ Error: "Invalid API key" หรือ "supabaseUrl is required"

**สาเหตุ**: ค่าใน `.env.local` ไม่ถูกต้องหรือไม่มีไฟล์

**แก้ไข**:
1. ตรวจสอบว่ามีไฟล์ `.env.local` ในโฟลเดอร์ root
2. ตรวจสอบว่าค่าถูกต้อง (ไปดูใน Supabase Dashboard > Settings > API)
3. **Restart dev server** (หยุดแล้วรัน `npm run dev` ใหม่)

---

### ❌ Authentication ไม่ทำงาน / ต้องการยืนยันอีเมล

**สาเหตุ**: Email confirmation เปิดอยู่

**แก้ไข**:
1. Supabase Dashboard > Authentication > Settings
2. ปิด "Enable email confirmations"
3. หรือใช้อีเมลจริงและตรวจสอบใน inbox

---

### ❌ Build Error: "Your project's URL and API key are required"

**สาเหตุ**: ตอน build ไม่มี environment variables

**แก้ไข**: ใช้ placeholder values ตอน build

```bash
SUPABASE_URL=https://placeholder.supabase.co SUPABASE_ANON_KEY=placeholder npm run build
```

---

### ❌ CORS Error / Connection Error

**สาเหตุ**: Supabase project ยังไม่พร้อมหรือ API key ผิด

**แก้ไข**:
1. รอให้ Supabase project setup เสร็จ (ประมาณ 2 นาที)
2. ตรวจสอบว่า project status เป็น "Active" (เขียว)
3. ตรวจสอบ API keys อีกครั้ง

---

## 📊 Supabase Free Tier Limits

| Resource | Limit |
|----------|-------|
| Database Storage | 500 MB |
| Bandwidth | 5 GB |
| API Requests | Unlimited |
| Authentication Users | Unlimited |
| File Storage | 1 GB |
| Realtime Connections | 200 concurrent |

**เพียงพอสำหรับ**:
- โปรเจกต์ส่วนตัว
- Prototype
- Small apps
- Learning/Testing

---

## 🎯 ขั้นตอนถัดไป

1. **เพิ่ม Features**:
   - Real-time subscriptions
   - File upload with Storage
   - Row Level Security ขั้นสูง

2. **ปรับปรุง UI**:
   - Loading states
   - Error handling
   - Animations

3. **Deploy**:
   - Deploy บน Vercel (ฟรี)
   - ตั้งค่า Environment Variables ใน Vercel Dashboard
   - Connect domain (optional)

---

## 🔗 ลิงก์ที่เป็นประโยชน์

- [Supabase Dashboard](https://app.supabase.com)
- [Supabase Docs](https://supabase.com/docs)
- [Next.js Docs](https://nextjs.org/docs)
- [Tailwind CSS Docs](https://tailwindcss.com/docs)

---

## 💡 Tips & Best Practices

1. **ใช้ Row Level Security (RLS)** เสมอ - ปลอดภัยกว่า
2. **ตั้งชื่อตารางและ column เป็นภาษาอังกฤษ** - หลีกเลี่ยงปัญหา encoding
3. **ใช้ TypeScript types** - สร้างจาก Supabase CLI หรือ manually
4. **เก็บ Service Role Key ไว้ที่ server** - อย่าใช้ในฝั่ง client
5. **Monitor usage** ใน Supabase Dashboard - ดู bandwidth และ storage

---

มีคำถามหรือปัญหา? สร้าง Issue หรือ Discussion บน GitHub! 🚀

