# 🎯 Affiliate System

ระบบ Affiliate สำหรับแพลตฟอร์มมวยไทยที่ช่วยให้ผู้ใช้สามารถแนะนำเพื่อนและรับแต้มสะสม

## ✨ ฟีเจอร์หลัก

### 1. หน้า Affiliate หลัก (`/affiliate`)
- แสดงสถิติการแนะนำ
- สร้างและจัดการลิงก์แนะนำ
- แชร์บนโซเชียลมีเดีย
- ดูประวัติการแนะนำ

### 2. Affiliate Dashboard (`/dashboard/affiliate`)
- สถิติแบบละเอียด
- การเติบโตรายเดือน
- เป้าหมายและความคืบหน้า
- ประวัติการแนะนำแบบละเอียด

### 3. ระบบ Referral Code
- รูปแบบ: `MT` + 8 ตัวอักษร (เช่น `MT12345678`)
- สร้างอัตโนมัติจาก User ID
- ตรวจสอบความถูกต้อง

### 4. การให้แต้ม
- 200 แต้มต่อการแนะนำ 1 คน
- เชื่อมต่อกับระบบ Gamification
- ติดตามใน `points_history`

## 🔧 API Endpoints

### GET `/api/affiliate`
ดึงข้อมูลสถิติ Affiliate ของผู้ใช้

**Response:**
```json
{
  "totalReferrals": 5,
  "totalEarnings": 1000,
  "currentMonthReferrals": 2,
  "conversionRate": 50,
  "referralHistory": [...]
}
```

### POST `/api/affiliate`
ให้แต้มเมื่อมีการแนะนำสำเร็จ

**Request:**
```json
{
  "referredUserId": "uuid",
  "referralCode": "MT12345678"
}
```

### POST `/api/affiliate/validate`
ตรวจสอบความถูกต้องของ Referral Code

**Request:**
```json
{
  "code": "MT12345678"
}
```

**Response:**
```json
{
  "valid": true,
  "referrer": {
    "id": "uuid",
    "name": "John Doe",
    "email": "john@example.com"
  },
  "message": "คุณถูกแนะนำโดย John Doe"
}
```

## 🎮 การใช้งาน

### สำหรับผู้ใช้ทั่วไป
1. เข้าไปที่ `/affiliate`
2. คัดลอกลิงก์หรือโค้ดแนะนำ
3. แชร์ให้เพื่อน
4. รับแต้มเมื่อเพื่อนสมัครสมาชิก

### สำหรับผู้สมัครใหม่
1. ใช้ลิงก์แนะนำหรือกรอกโค้ดในฟอร์มสมัคร
2. ระบบจะให้แต้มกับผู้แนะนำอัตโนมัติ
3. ดูประวัติใน Affiliate Dashboard

## 📊 สถิติที่ติดตาม

- **Total Referrals**: จำนวนผู้แนะนำทั้งหมด
- **Total Earnings**: แต้มสะสมทั้งหมด
- **Current Month**: ผู้แนะนำในเดือนนี้
- **Conversion Rate**: อัตราการแปลง
- **Monthly Growth**: การเติบโตรายเดือน

## 🔗 การเชื่อมต่อกับระบบอื่น

### Gamification System
- ใช้ `points_history` table
- เชื่อมต่อกับ `user_points`
- รองรับ badges และ achievements

### User Authentication
- ใช้ Supabase Auth
- เชื่อมต่อกับ `profiles` table
- รองรับ role-based access

## 🚀 การพัฒนาต่อ

### ฟีเจอร์ที่อาจเพิ่มในอนาคต
1. **Commission System**: ระบบค่าคอมมิชชั่น
2. **Tier System**: ระดับ Affiliate
3. **Analytics**: การวิเคราะห์แบบละเอียด
4. **Email Templates**: เทมเพลตอีเมลแนะนำ
5. **Social Sharing**: การแชร์แบบ advanced

### การปรับปรุง
1. **Performance**: Cache สถิติ
2. **Security**: Rate limiting
3. **UX**: Toast notifications
4. **Mobile**: Responsive design

## 📝 หมายเหตุ

- ระบบใช้ Supabase เป็น backend
- รองรับการทำงานแบบ real-time
- เชื่อมต่อกับระบบ Gamification ที่มีอยู่
- ใช้ HeroUI components สำหรับ UI
