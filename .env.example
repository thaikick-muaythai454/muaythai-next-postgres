# Supabase Configuration
# Get these from your Supabase project settings at https://app.supabase.com

# NEXT_PUBLIC_* variables are exposed to the browser (safe for anon key)
# Required for client-side authentication
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Regular env vars are server-side only (NOT exposed to browser)
# Required for server-side operations
SUPABASE_URL=your-project-url
SUPABASE_ANON_KEY=your-anon-key

# Stripe Configuration
# Get these from your Stripe dashboard at https://dashboard.stripe.com/apikeys
# STRIPE_PUBLISHABLE_KEY is exposed to browser
STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key

# Secret key is server-side only (NEVER expose to browser)
STRIPE_SECRET_KEY=your-stripe-secret-key

# Webhook secret for verifying Stripe webhook events
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret

# App URL for redirects and webhooks
NEXT_PUBLIC_APP_URL=http://localhost:3000
