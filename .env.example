# ============================================================================
# THAIKICK Platform - Environment Variables Template
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control
#
# Usage:
#   cp .env.example .env.local
#   # Then edit .env.local with your actual values
# ============================================================================

# ----------------------------------------------------------------------------
# Supabase Configuration (Required)
# ----------------------------------------------------------------------------
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Client-side (exposed to browser - required for build)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-side (optional - can use NEXT_PUBLIC_ versions if same)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key (for admin operations - keep secret!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ----------------------------------------------------------------------------
# Stripe Configuration (Required for Payments)
# ----------------------------------------------------------------------------
# Get these from: https://dashboard.stripe.com/apikeys

# Public key (exposed to browser - required for build)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Secret key (server-side only - keep secret!)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Webhook secret (for verifying Stripe webhooks)
# Get from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ----------------------------------------------------------------------------
# Google Analytics (Optional - for tracking)
# ----------------------------------------------------------------------------
# Get from: https://analytics.google.com/
# Measurement ID format: G-XXXXXXXXXX

NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Optional: Google Ads Conversion ID (if using Google Ads)
# NEXT_PUBLIC_GA_CONVERSION_ID=AW-XXXXXXXXXX

# ----------------------------------------------------------------------------
# Google Maps API (Optional)
# ----------------------------------------------------------------------------
# Get from: https://console.cloud.google.com/google/maps-apis
# Required if using Google Places API for reviews

GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
# OR (if you prefer client-side)
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# ----------------------------------------------------------------------------
# Email Configuration (Resend)
# ----------------------------------------------------------------------------
# Get from: https://resend.com/api-keys
# Required for sending emails (booking confirmations, receipts, etc.)

RESEND_API_KEY=re_your_resend_api_key_here

# Email addresses
CONTACT_EMAIL_FROM=noreply@yourdomain.com
CONTACT_EMAIL_TO=admin@yourdomain.com

# ----------------------------------------------------------------------------
# Application URLs
# ----------------------------------------------------------------------------
# Base URL for your application (used in emails, redirects, etc.)

NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# For production, use your actual domain:
# NEXT_PUBLIC_APP_URL=https://yourdomain.com
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# ----------------------------------------------------------------------------
# Cron Jobs Security (Optional but Recommended)
# ----------------------------------------------------------------------------
# Secret key for authenticating cron job requests
# Generate a random string: openssl rand -base64 32

CRON_SECRET=your-random-secret-key-here

# ----------------------------------------------------------------------------
# Node Environment
# ----------------------------------------------------------------------------
# Automatically set by Next.js, but you can override:
# NODE_ENV=development

# ============================================================================
# Notes:
# ============================================================================
# 1. Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
#    - Only use for non-sensitive data
#    - Must be available at build time
#
# 2. Variables WITHOUT NEXT_PUBLIC_ prefix are server-side only
#    - More secure - not exposed to browser
#    - Can be runtime or build-time
#
# 3. For production deployment (Vercel, etc.):
#    - Add all variables in your platform's environment settings
#    - NEXT_PUBLIC_ variables must be set before build
#    - Regular variables can be set at runtime
#
# 4. Security:
#    - NEVER commit .env.local or .env to version control
#    - Use strong, random values for secrets
#    - Rotate keys regularly
#    - Use different keys for development and production
#
# ============================================================================