# ============================================================================
# Muay Thai Platform - Environment Variables Template
# ============================================================================
# 
# Copy this file to .env.local and fill in your actual values
# 
# Usage:
#   cp .env.example .env.local
#   # Then edit .env.local with your actual values
#
# ⚠️ IMPORTANT: Never commit .env.local to git (it's in .gitignore)
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION (Required)
# ============================================================================

# Client-side Supabase Configuration (Required for build)
# These are exposed to the browser, so they must be public
# Get these from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Server-side Supabase Configuration (Optional - can use NEXT_PUBLIC_ versions)
# These are NOT exposed to the browser (more secure)
# If not set, will fallback to NEXT_PUBLIC_ versions
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (Optional - for admin operations)
# ⚠️ WARNING: This key has full access - keep it SECRET!
# Get from: Supabase Dashboard > Project Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# APPLICATION URL (Required)
# ============================================================================

# Base URL of your application
# For local development: http://localhost:3000
# For production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================================
# STRIPE PAYMENT CONFIGURATION (Required for payments)
# ============================================================================

# Stripe Publishable Key (Client-side - exposed to browser)
# Get from: Stripe Dashboard > Developers > API keys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Stripe Secret Key (Server-side only - KEEP SECRET!)
# Get from: Stripe Dashboard > Developers > API keys
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Stripe Webhook Secret (Required for production webhooks)
# Get from: Stripe Dashboard > Developers > Webhooks > Add endpoint > Signing secret
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ============================================================================
# EMAIL SERVICE CONFIGURATION
# ============================================================================

# ----------------------------------------------------------------------------
# Resend (Recommended - Default)
# ----------------------------------------------------------------------------
# Resend is used as the default email provider
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Email sender address (for Resend)
# For development: onboarding@resend.dev (no verification needed)
# For production: noreply@yourdomain.com (must verify domain first)
CONTACT_EMAIL_FROM=onboarding@resend.dev

# Email recipient address (where contact form emails go)
CONTACT_EMAIL_TO=admin@yourdomain.com

# ----------------------------------------------------------------------------
# Google SMTP (Fallback - Optional)
# ----------------------------------------------------------------------------
# Google SMTP is used as fallback when Resend is not configured
# Only needed if you don't want to use Resend
# 
# Setup instructions:
# 1. Enable 2-Step Verification on your Google Account
# 2. Create App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password (not your regular password)

# SMTP Server Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
# SMTP_PORT=465
# SMTP_SECURE=true

# SMTP Authentication
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password

# Email sender address (for SMTP)
SMTP_FROM=your-email@gmail.com

# ============================================================================
# GOOGLE ANALYTICS (Optional)
# ============================================================================

# Google Analytics 4 Measurement ID
# Get from: Google Analytics > Admin > Data Streams > Measurement ID
# Format: G-XXXXXXXXXX
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Google Analytics Conversion ID (Optional)
# For tracking conversions (e.g., purchases, signups)
NEXT_PUBLIC_GA_CONVERSION_ID=AW-XXXXXXXXXX

# ============================================================================
# CRON JOBS SECURITY (Required for production)
# ============================================================================

# Secret key for protecting cron job endpoints
# Generate a random secure string (e.g., openssl rand -hex 32)
# This prevents unauthorized access to cron endpoints
CRON_SECRET=your-random-secret-key-here

# ============================================================================
# NODE ENVIRONMENT
# ============================================================================

# Environment mode
# Options: development, production, test
# Usually set automatically by Next.js, but can be set manually
# NODE_ENV=development

# ============================================================================
# DEVELOPMENT NOTES
# ============================================================================
#
# Local Development:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your Supabase credentials (get from: supabase status)
# 3. Fill in Stripe test keys (get from: Stripe Dashboard)
# 4. Optional: Set up Resend for email (recommended)
# 5. Optional: Set up Google Analytics for tracking
# 6. Restart dev server: npm run dev
#
# Production:
# 1. Set all required variables in your deployment platform (Vercel, etc.)
# 2. Make sure NEXT_PUBLIC_* variables are available at BUILD TIME
# 3. Set CRON_SECRET for cron job security
# 4. Use production Stripe keys (not test keys)
# 5. Verify Resend domain for production emails
# 6. Redeploy after setting variables
#
# ============================================================================
# REQUIRED vs OPTIONAL
# ============================================================================
#
# ✅ REQUIRED (Must be set):
#   - NEXT_PUBLIC_SUPABASE_URL
#   - NEXT_PUBLIC_SUPABASE_ANON_KEY
#   - NEXT_PUBLIC_APP_URL
#   - NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY (if using payments)
#   - STRIPE_SECRET_KEY (if using payments)
#
# ⚠️ OPTIONAL (Recommended):
#   - RESEND_API_KEY (recommended for email)
#   - CONTACT_EMAIL_FROM
#   - CONTACT_EMAIL_TO
#   - CRON_SECRET (required for production)
#
# 📊 OPTIONAL (Nice to have):
#   - NEXT_PUBLIC_GA_MEASUREMENT_ID (for analytics)
#   - SUPABASE_SERVICE_ROLE_KEY (for admin operations)
#   - SMTP_* (only if not using Resend)
#
# ============================================================================