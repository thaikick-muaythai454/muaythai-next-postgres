# ============================================================================
# THAIKICK - Environment Variables Configuration
# ============================================================================
# คัดลอกไฟล์นี้ไปยัง .env.local และตั้งค่าตัวแปรที่จำเป็น
# Copy this file to .env.local and set the required variables
#
# Usage:
#   cp .env.example .env.local
#   # Then edit .env.local with your actual values
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION (Required)
# ============================================================================
# Get these from: https://app.supabase.com/project/_/settings/api

# Public Supabase URL (exposed to browser)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Public Supabase Anonymous Key (exposed to browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Server-side Supabase URL (not exposed to browser - more secure)
# Can be same as NEXT_PUBLIC_SUPABASE_URL
SUPABASE_URL=your_supabase_project_url

# Server-side Supabase Anonymous Key (not exposed to browser - more secure)
# Can be same as NEXT_PUBLIC_SUPABASE_ANON_KEY
SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (server-side only - has admin privileges)
# ⚠️ NEVER expose this to the browser!
# Get from: https://app.supabase.com/project/_/settings/api
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ============================================================================
# STRIPE CONFIGURATION (Required for payment features)
# ============================================================================
# Get these from: https://dashboard.stripe.com/apikeys

# Stripe Publishable Key (exposed to browser)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Stripe Secret Key (server-side only - NEVER expose to browser!)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Stripe Webhook Secret (for webhook signature verification)
# Get from: https://dashboard.stripe.com/webhooks
# ⚠️ This is required for production, optional for development
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ============================================================================
# EMAIL CONFIGURATION (Required for contact form and notifications)
# ============================================================================
# Using Resend service: https://resend.com

# Resend API Key
# Get from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here

# Default sender email address
# Must be verified in Resend dashboard
FROM_EMAIL=noreply@yourdomain.com

# Contact form email settings
CONTACT_EMAIL_FROM=noreply@yourdomain.com
CONTACT_EMAIL_TO=admin@yourdomain.com

# ============================================================================
# APP CONFIGURATION (Required)
# ============================================================================

# Application URL (used for redirects and links)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node Environment
# Options: development, production, test
NODE_ENV=development

# ============================================================================
# GOOGLE MAPS CONFIGURATION (Optional - for location features)
# ============================================================================
# Get from: https://console.cloud.google.com/apis/credentials

# Google Maps API Key (for Google Places integration)
# Can use either GOOGLE_MAPS_API_KEY or NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ============================================================================
# DEVELOPMENT OPTIONS (Optional)
# ============================================================================
# These are only used in development mode

# Ignore ESLint errors during builds (development only)
# Set to 'true' to ignore ESLint errors (not recommended)
IGNORE_ESLINT_BUILD_ERRORS=false

# Ignore TypeScript errors during builds (development only)
# Set to 'true' to ignore TypeScript errors (not recommended)
# ⚠️ RECOMMENDED: Fix errors instead of ignoring them
IGNORE_TYPESCRIPT_BUILD_ERRORS=false

# ============================================================================
# NOTES
# ============================================================================
# 1. Never commit .env.local to version control (it's in .gitignore)
# 2. For production, use your hosting platform's environment variable settings
# 3. Stripe keys: Use test keys (pk_test_/sk_test_) for development,
#    live keys (pk_live_/sk_live_) for production
# 4. Always verify email addresses in Resend dashboard before using
# 5. Keep SUPABASE_SERVICE_ROLE_KEY and STRIPE_SECRET_KEY secret!
